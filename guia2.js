addEventListener("DOMContentLoaded", ()=>{
    //area del rectangulo
    let lad=Number(prompt(`ingrese el valor del rectangulo`))
    let area=lad*lad
    console.log(`area del rectangulo es ${area}`)


})
